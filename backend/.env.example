# Copy to .env — never commit the real file.
# Day 2+: used by the LLM agent layer. Leave empty to run in fully deterministic mode.
OPENAI_API_KEY=
# Deterministic mode guarantees identical demo runs even with the agent layer on.
KAVACH_DETERMINISTIC=1
